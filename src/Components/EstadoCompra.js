/* import React from 'react'
import Cart from './Cart'

const EstadoCompra = ({compra}) => {
  return (
    <div>EstadoCompra
       
        {compra.map((product, index) => (
        <Cart key={index}>{product}</Cart>))}
    </div>
  )
}

export default EstadoCompra */